# ROS <-> Gazebo Sim bridge for the base101 mobile base.
# /clock is bridged separately (in the launch file) so we can keep it in a
# dedicated single-topic bridge node.

# 2D laser from the gpu_lidar sensor on lidar_1.
- topic_name: "/scan"
  ros_type_name: "sensor_msgs/msg/LaserScan"
  gz_type_name: "gz.msgs.LaserScan"

# Camera info for head_camera. The image stream itself goes through the
# dedicated ros_gz_image bridge (started in the launch file).
- topic_name: "/head_camera/camera_info"
  ros_type_name: "sensor_msgs/msg/CameraInfo"
  gz_type_name: "gz.msgs.CameraInfo"
